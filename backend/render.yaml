services:
  - type: web
    name: locker-room
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: FRONTEND_URL
        value: https://locker-room-frontend.vercel.app
    healthCheckPath: /health
